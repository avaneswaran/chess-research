.venv/
corpus/
__pycache__/
*.pyc

# terraform
infra/.terraform/
# NOT .terraform.lock.hcl — that file pins provider versions and hashes,
# and committing it is the whole point of having it.
*.tfstate
*.tfstate.*
terraform.tfvars
crash.log

# vault lab: unseal keys and root token, never commit
vault/.init-keys.json
